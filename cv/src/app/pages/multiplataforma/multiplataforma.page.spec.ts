import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MultiplataformaPage } from './multiplataforma.page';

describe('MultiplataformaPage', () => {
  let component: MultiplataformaPage;
  let fixture: ComponentFixture<MultiplataformaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MultiplataformaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
